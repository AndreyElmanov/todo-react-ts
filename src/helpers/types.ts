export type TodoType = {
    userId?: number | string;
    id: number | string;
    title: string;
    completed: boolean;
}

export type UserDataType = {
    firstName: string;
    secondName: string;
    description: string;
}